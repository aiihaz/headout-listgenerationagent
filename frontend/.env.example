# Backend API URL (FastAPI — local or Render)
VITE_API_URL=http://localhost:8000

# Supabase — leave blank to run in dev/no-auth mode (filesystem fallback active)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
